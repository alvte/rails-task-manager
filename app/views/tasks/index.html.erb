<h1>Hello Bob</h1>
<h2>Please find below your list of task</h2>

<ul>
  <% @tasks.each do |task| %>
  <li>
    Task <%= task.id %> is <%= link_to task.title.downcase, task_path(task) %>, <%= task.details %>. Status : <%= task.completed %>
  </li>
  <% end %>
</ul>
